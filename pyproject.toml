[project]
name = "allen-cahn-certified-observer"
version = "0.1.0"
description = "High-accuracy Allen-Cahn solver and certified nonlinear observer correction under partial observations."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
